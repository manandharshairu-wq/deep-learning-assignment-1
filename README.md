# deep-learning-assignment-1
A PyTorch-based benchmark comparing MLP, CNN, and attention models on tabular (Adult) and image (CIFAR-100 &amp; PCam) datasets, analyzing how architecture choice affects performance.
